- name: Install apt packages
  become: yes
  apt:
    name: "{{ packages }}"
  vars:
    packages:
    - bear
    - clang-format
    - clangd-14
    - g++-10
- name: Setup clangd symlink
  become: yes
  file:
    src: /usr/bin/clangd-14
    dest: /usr/bin/clangd
    state: link
