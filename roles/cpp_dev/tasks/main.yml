- name: Install apt packages
  become: yes
  apt:
    name: "{{ packages }}"
  vars:
    packages:
    - bear
    - clang-format
    - clangd-10
    - g++-10
- name: Setup clangd symlink
  become: yes
  file:
    src: /usr/bin/clangd-10
    dest: /usr/bin/clangd
    state: link
