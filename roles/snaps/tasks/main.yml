- name: Add sandboxed snaps
  become: yes
  snap:
    name: "{{ packages }}"
  vars:
    packages:
      - spotify
      - discord
- name: IntelliJ
  become: yes
  snap:
    name: intellij-idea-community
    classic: yes
