- name: Create destinations
  ansible.builtin.file:
    dest: "{{ item }}"
    state: directory
    mode: "0755"
  loop:
    - "{{ home }}/.local/share/oss-cad-suite"
    - "{{ home }}/.local/share/vhdl-ls"
- name: Download oss-cad-suite
  ansible.builtin.get_url:
    url: https://github.com/YosysHQ/oss-cad-suite-build/releases/download/2024-12-18/oss-cad-suite-linux-x64-20241218.tgz
    dest: "{{ home }}/.local/share/oss-cad-suite-linux-x64-20241218.tgz"
    checksum: sha256:733ca13b5156e019baca8c9d84bdd1bae6e79eeefc58c0048cc15332fe2de61d
    mode: "0644"
  register: cad_download
- name: Install oss-cad-suite
  ansible.builtin.unarchive:
    src: "{{ cad_download.dest }}"
    dest: "{{ home }}/.local/share"
  when: cad_download.changed
- name: Download VHDL LS
  ansible.builtin.get_url:
    url: https://github.com/VHDL-LS/rust_hdl/releases/download/v0.83.1/vhdl_ls-x86_64-unknown-linux-gnu.zip
    dest: "{{ home }}/.local/share/vhdl_ls-0.83.1.zip"
    checksum: sha256:fbff90714c35f61a620ef8801660773d2034e97d8428dc2ce006a39ed10bb1e3
    mode: "0644"
  register: vhdl_ls_download
- name: Install VHDL LS
  ansible.builtin.unarchive:
    src: "{{ vhdl_ls_download.dest }}"
    dest: "{{ home }}/.local/share/vhdl-ls/"
  when: vhdl_ls_download.changed
- name: Link VHDL LS
  ansible.builtin.file:
    src: "{{ home }}/.local/share/vhdl-ls/vhdl_ls-x86_64-unknown-linux-gnu/bin/vhdl_ls"
    dest: "{{ home }}/.local/bin/vhdl_ls"
    state: link
    force: true
