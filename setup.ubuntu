#!/bin/bash

set -ex

if [ -z "$NO_BECOME_PROMPT" ]
then
    PROMPT=-K
fi

if ! command -v ansible
then
    sudo apt-get install -y ansible cowsay
fi

. dotfiles/profile

ANSIBLE_STDOUT_CALLBACK=debug ansible-playbook -i hosts.ini -l localhost playbook.yml $PROMPT
