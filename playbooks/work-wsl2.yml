- hosts:
   - localhost
  vars:
    home: "{{ lookup('env', 'HOME') }}"
    user: "{{ lookup('env', 'USER') }}"
    git_email: "jennifer.wilcox@adtran.com"
    is_wsl2: true
  connection: local
  roles:
    - common
    - rust_dev
    - cpp_dev
    - fpga_dev
    - gui
