- hosts:
   - localhost
  vars:
    home: "{{ lookup('env', 'HOME') }}"
    user: "{{ lookup('env', 'USER') }}"
  connection: local
  roles:
    - common
    - docker
    - rust_dev
    - cpp_dev
    - haskell_dev
    - gui
    - 3d_printing
