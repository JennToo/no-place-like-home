- hosts:
   - localhost
  vars:
    home: "{{ lookup('env', 'HOME') }}"
    user: "{{ lookup('env', 'USER') }}"
    git_email: "jennifer@nitori.org"
    gui_packages: true
    is_wsl2: true
  connection: local
  roles:
    - common
    - wsl
    - xrdp
    - docker
    - rust_dev
    - cpp_dev
    - lisp_dev
    - fpga_dev
    - sway
    - gui
    - 3d_printing
