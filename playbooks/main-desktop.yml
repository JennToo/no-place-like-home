- name: Desktop Linux
  hosts:
   - localhost
  vars:
   home: "{{ lookup('env', 'HOME') }}"
   user: "{{ lookup('env', 'USER') }}"
   git_email: "jennifer@nitori.org"
  connection: local
  roles:
   - common
   - fpga_dev
   - docker
   - rust_dev
   - gui
   - music
