- hosts:
   - localhost
  vars:
    home: "{{ lookup('env', 'HOME') }}"
    user: "{{ lookup('env', 'USER') }}"
  connection: local
  roles:
    - common
    - docker
    - bazel
    - re
    - rust_dev
    - lisp_dev
    - fpga_dev
    - cpp_dev
    - gba_dev
    - cx16
    - gui
    - snaps
    - 3d_printing
    - games
