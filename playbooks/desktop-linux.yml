- hosts:
   - localhost
  vars:
    home: "{{ lookup('env', 'HOME') }}"
    user: "{{ lookup('env', 'USER') }}"
  connection: local
  roles:
    - common
    - bazel
    - re
    - js_dev
    - fpga_dev
    - cpp_dev
    - scala_dev
    - gba_dev
    - cx16
    - gui
    - snaps
